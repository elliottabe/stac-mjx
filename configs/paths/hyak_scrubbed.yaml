user: eabe

# set the directory where the output files get saved
cwd_dir:  /gscratch/portia/${paths.user}/Research/MyRepos/Fly_tracking
base_dir: /gscratch/scrubbed/portia/${paths.user}/biomech_model/${dataset.name}/RL_${dataset.name}/${version}
save_dir: ${paths.base_dir}/run_id=${run_id}
log_dir:  ${paths.save_dir}/logs/
ckpt_dir: ${paths.save_dir}/ckpt/
fig_dir:  ${paths.save_dir}/figures/
data_dir: /gscratch/portia/${paths.user}/biomech_model/${dataset.name}/datasets/Tuthill_data/